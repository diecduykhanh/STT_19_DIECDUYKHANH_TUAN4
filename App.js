import { StatusBar } from 'expo-status-bar';
import { StyleSheet, Text, View, Button, TouchableOpacity, Pressable } from 'react-native';
import LoginScreen from './src/components/LoginScreen';
import PasswordGenerateScreen from './src/components/PasswordGenerateScreen';
import VoteScreen from './src/components/VoteScreen';
import PayScreen from './src/components/PayScreen';

export default function App({ }) {
  return (
    //<LoginScreen/>
    //<VoteScreen/>
    //<PasswordGenerateScreen/>
    <PayScreen />
  );
}

const styles = StyleSheet.create({});
